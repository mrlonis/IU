class Point {
  int x, y;
  
  Point (int x, int y) {
    this.x = x;
    this.y = y;
  }
  
  Point () {
    this.x = 0;
    this.y = 0;
  }
  
  int distance (Point p) {
    return Math.sqrt(Math.pow((this.x + p.x), 2) + Math.pow((this.y + p.y), 2));
  }
}